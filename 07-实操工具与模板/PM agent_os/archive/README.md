# Agent OS Archive — 旧代码参考

这些 Python 文件是 Agent OS v1.0 的代码实现，保留作为参考。

v2.0 已经转变为**纯框架驱动**的架构：
- 不再需要运行 Python 代码
- 不再需要 API Key
- 打开 VS Code、打开文件夹、和 AI 对话即可

如果你想了解每个模块的设计思路，可以参考对应的 Python 文件。
它们的逻辑已经被提炼到 `workflow/` 目录的 Markdown 文件中。
